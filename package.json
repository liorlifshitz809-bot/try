{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "dev:api": "PORT=${API_SERVER_PORT:-8080} pnpm --filter @workspace/api-server dev",
    "dev:web": "pnpm --filter @workspace/piano-conservatory dev",
    "dev": "pnpm -r --parallel --filter @workspace/api-server --filter @workspace/piano-conservatory dev",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "tunnel": "ngrok http --host-header=rewrite ${PORT:-5173}"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
